CREATE TABLE customer(
                       ID int NOT NULL AUTO_INCREMENT,
                       name varchar(200),
                       address varchar(250),
                       PRIMARY KEY (ID)
)ENGINE = INNODB